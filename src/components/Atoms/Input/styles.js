import { StyleSheet } from "aphrodite/no-important";

export default StyleSheet.create({
  input: {
    fontWeight: 500,
    fontSize: 13,
    padding: "15px 0",
    border: 0,
    width: "100%"
  }
});
