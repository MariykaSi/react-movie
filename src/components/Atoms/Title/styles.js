import { StyleSheet } from "aphrodite/no-important";

export default StyleSheet.create({
  title: {
    display: "block",
    fontWeight: "bold",
    margin: "15px 0 15px"
  },

  label: {
    fontWeight: "bold"
  }
});
